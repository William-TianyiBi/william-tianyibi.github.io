pages:
# NOTE: `name` field must match the `navbar_title` field in the page's front matter to ensure the correct page is highlighted in the navbar.
#       For example, the `navbar_title` field in a page should match the `name` field here.
- name: Home
  url: /
- name: Publications
  url: /publications
- name: Certificates
  url: /certificates
